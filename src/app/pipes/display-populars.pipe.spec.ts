import { DisplayPopularsPipe } from './display-populars.pipe';

describe('DisplayPopularsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayPopularsPipe();
    expect(pipe).toBeTruthy();
  });
});
